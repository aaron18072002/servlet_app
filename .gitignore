# Bỏ qua các thư mục và tệp của Tomcat trong thư mục Servers
/Servers/logs/
/Servers/work/
/Servers/temp/
/Servers/*.log
/Servers/*.war

# Bỏ qua thư mục và tệp biên dịch của Servlet project
/ServletApp/bin/
/ServletApp/target/
/ServletApp/build/

# Bỏ qua các tệp của Web Dynamic Application version 4.0
/ServletApp/.settings/
/ServletApp/.project
/ServletApp/.classpath
/ServletApp/.deploy/
*.war
*.ear

# Bỏ qua thư mục và tệp metadata ở cấp gốc của dự án
/.metadata/

# Bỏ qua thư mục và tệp metadata của các IDE khác (Eclipse, IntelliJ, NetBeans)
/ServletApp/.idea/
/ServletApp/.vscode/
/ServletApp/.project
/ServletApp/.metadata
/ServletApp/*.iml
/ServletApp/nbproject/

# Bỏ qua các tệp tạm thời và hệ thống
/ServletApp/*.swp
/ServletApp/tmp/
/ServletApp/.DS_Store

# Bỏ qua các tệp cấu hình không cần thiết của Maven hoặc Gradle
/ServletApp/.mvn/
/ServletApp/*.jar

# Bỏ qua các tệp cấu hình tài khoản người dùng
/ServletApp/.env

# Bỏ qua thư mục và tệp biên dịch của JDBC project
/JDBCBasics/bin/
/JDBCBasics/.settings/

# Bỏ qua thư mục và tệp biên dịch của Servlet project
/UserApp/bin/
/UserApp/target/
/UserApp/build/

# Bỏ qua các tệp của Web Dynamic Application version 4.0
/UserApp/.settings/
/UserApp/.project
/UserApp/.classpath
/UserApp/.deploy/


# Bỏ qua thư mục và tệp metadata của các IDE khác (Eclipse, IntelliJ, NetBeans)
/UserApp/.idea/
/UserApp/.vscode/
/UserApp/.project
/UserApp/.metadata
/UserApp/*.iml
/UserApp/nbproject/

# Bỏ qua các tệp tạm thời và hệ thống
/UserApp/*.swp
/UserApp/tmp/
/UserApp/.DS_Store

# Bỏ qua các tệp cấu hình không cần thiết của Maven hoặc Gradle
/UserApp/.mvn/
/UserApp/*.jar

# Bỏ qua các tệp cấu hình tài khoản người dùng
/UserApp/.env


# ------------------------------

# Bỏ qua thư mục và tệp biên dịch của Servlet project
/PreparedStatement/bin/
/PreparedStatement/target/
/PreparedStatement/build/

# Bỏ qua các tệp của Web Dynamic Application version 4.0
/PreparedStatement/.settings/
/PreparedStatement/.project
/PreparedStatement/.classpath
/PreparedStatement/.deploy/


# Bỏ qua thư mục và tệp metadata của các IDE khác (Eclipse, IntelliJ, NetBeans)
/PreparedStatement/.idea/
/PreparedStatement/.vscode/
/PreparedStatement/.project
/PreparedStatement/.metadata
/PreparedStatement/*.iml
/PreparedStatement/nbproject/

# Bỏ qua các tệp tạm thời và hệ thống
/PreparedStatement/*.swp
/PreparedStatement/tmp/
/PreparedStatement/.DS_Store

# Bỏ qua các tệp cấu hình không cần thiết của Maven hoặc Gradle
/PreparedStatement/.mvn/
/PreparedStatement/*.jar

# Bỏ qua các tệp cấu hình tài khoản người dùng
/PreparedStatement/.PreparedStatement

#------------------------------------

# Bỏ qua thư mục và tệp biên dịch của Servlet project
/InterServletCommunication/bin/
/InterServletCommunication/target/
/InterServletCommunication/build/

# Bỏ qua các tệp của Web Dynamic Application version 4.0
/InterServletCommunication/.settings/
/InterServletCommunication/.project
/InterServletCommunication/.classpath
/InterServletCommunication/.deploy/


# Bỏ qua thư mục và tệp metadata của các IDE khác (Eclipse, IntelliJ, NetBeans)
/InterServletCommunication/.idea/
/InterServletCommunication/.vscode/
/InterServletCommunication/.project
/InterServletCommunication/.metadata
/InterServletCommunication/*.iml
/InterServletCommunication/nbproject/

# Bỏ qua các tệp tạm thời và hệ thống
/InterServletCommunication/*.swp
/InterServletCommunication/tmp/
/InterServletCommunication/.DS_Store

# Bỏ qua các tệp cấu hình không cần thiết của Maven hoặc Gradle
/InterServletCommunication/.mvn/
/InterServletCommunication/*.jar

# Bỏ qua các tệp cấu hình tài khoản người dùng
/InterServletCommunication/.PreparedStatement

# -----------------------------------

# Bỏ qua thư mục và tệp biên dịch của Servlet project
/*/bin/
/*/target/
/*/build/

# Bỏ qua các tệp của Web Dynamic Application version 4.0
/*/.settings/
/*/.project
/*/.classpath
/*/.deploy/


# Bỏ qua thư mục và tệp metadata của các IDE khác (Eclipse, IntelliJ, NetBeans)
/*/.idea/
/*/.vscode/
/*/.project
/*/.metadata
/*/*.iml
/*/nbproject/

# Bỏ qua các tệp tạm thời và hệ thống
/*/*.swp
/*/tmp/
/*/.DS_Store

# Bỏ qua các tệp cấu hình không cần thiết của Maven hoặc Gradle
/*/.mvn/
/*/*.jar